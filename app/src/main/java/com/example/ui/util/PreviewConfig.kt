package com.example.ui.util

import com.example.util.PreviewConfig as MainPreviewConfig

/**
 * UI package typealias / delegate for [MainPreviewConfig] to support imports from `com.example.ui.util.PreviewConfig`.
 */
val PreviewConfig = MainPreviewConfig
